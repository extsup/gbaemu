cmake_minimum_required(VERSION 3.22.1)
project(gpsp_jni)
# Add the upstream gPSP sources at app/src/main/cpp/gpsp, then list them here.
# add_library(gpsp_jni SHARED gpsp_jni.cpp <upstream gPSP source files>)
# target_link_libraries(gpsp_jni android log GLESv2)
